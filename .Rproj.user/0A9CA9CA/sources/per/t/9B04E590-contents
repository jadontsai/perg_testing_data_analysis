

scores <- read.csv("perg_scores.csv")
names(scores)
scores$Totaldistance. <- as.numeric(scores$Totaldistance)
scores$Averagepower. <- as.numeric(scores$Averagepower)


plot(scores$Totaldistance, scores$Averagepower, 
     main="Distance vs Average power", xlab="Distance (m)", ylab="Average Power (W)", col="blue", pch=19)
